/**
 * GTM dataLayer Bootstrap
 *
 * This file is loaded via <Script src="/gtm-bootstrap.js" ... />
 * (an external <script src=…> element — NOT inline).
 *
 * CSP: because it is served from 'self' (same-origin) and there is no
 * inline JS inside the file, no nonce or 'unsafe-inline' is required.
 */
window.dataLayer = window.dataLayer || [];
